import Agenda from "agenda";

export default new Agenda({
  // Uncomment this line and provide valid URL
  // if you have any DB server running
  // db: { address: process.env.DB_CONNECTION_URL },
});
